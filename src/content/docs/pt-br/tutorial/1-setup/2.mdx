---
type: tutorial
title: Crie seu primeiro projeto Astro
description: |-
  Tutorial: Construa seu primeiro blog com Astro —
  Crie um novo projeto para o tutorial do Astro e esteja pronto para programar
i18nReady: true
---
import Badge from '~/components/Badge.astro';
import Checklist from '~/components/Checklist.astro';
import Box from '~/components/tutorial/Box.astro';
import InstallGuideTabGroup from '~/components/TabGroup/InstallGuideTabGroup.astro';
import PackageManagerTabs from '~/components/tabs/PackageManagerTabs.astro';
import PreCheck from '~/components/tutorial/PreCheck.astro';



<PreCheck>
  - Execute o assistente de configuração `create astro` para criar um novo projeto Astro
  - Inicie o servidor Astro em modo de desenvolvimento (dev)
  - Visualize uma prévia ao vivo do seu website no seu navegador
</PreCheck>

## Inicie o assistente de configuração Astro

A forma preferida de criar um novo site Astro é através do nosso assistente de configuração `create astro`.

1. No terminal, execute o seguinte comando usando o seu gerenciador de pacotes preferido:

    <PackageManagerTabs>
      <Fragment slot="npm">
      ```shell
      # criar um novo projeto com o npm
      npm create astro@latest
      ```
      </Fragment>
      <Fragment slot="pnpm">
      ```shell
      # criar um novo projeto com o pnpm
      pnpm create astro@latest
      ```
      </Fragment>
      <Fragment slot="yarn">
      ```shell
      # criar um novo projeto com o yarn
      yarn create astro
      ```
      </Fragment>
    </PackageManagerTabs>

2. Confirme com `y` para instalar o `create-astro`.
3. Quando o prompt perguntar, "Onde você gostaria de criar o seu novo projeto?", digite o nome de uma pasta para criar um novo diretório para o seu projeto, por exemplo,
`./tutorial`

    :::note
    Um novo projeto Astro só pode ser criado em uma pasta completamente vazia, portanto, escolha um nome para a sua pasta que ainda não exista!
    :::

4. Você verá uma lista curta de modelos de início disponíveis para escolher. Use as setas (para cima e para baixo) para navegar até o modelo "Empty" e pressione a tecla Enter para confirmar sua escolha.

5. Quando o prompt perguntar, "Deseja instalar as dependências?", digite `y`.

6. Quando o prompt perguntar se você pretende usar TypeScript, digite `n`.

7. Quando o prompt perguntar, "Deseja inicializar um novo repositório git?", digite `y`.

Quando o assistente de instalação for concluído, você não precisará mais deste terminal. Agora você pode abrir o VS Code para continuar.

## Abra o seu projeto no VS Code

1. Abra o VS Code. Será solicitado que você abra uma pasta. Escolha a pasta que você criou durante o assistente de configuração.

2. Se esta for a primeira vez que você abre um projeto Astro, você verá uma notificação perguntando se deseja instalar as extensões recomendadas. Clique para ver as extensões recomendadas e escolha a [extensão de suporte à linguagem Astro](https://marketplace.visualstudio.com/items?itemName=astro-build.astro-vscode). Isso fornecerá realce de sintaxe e autocompletar para o seu código Astro.

3. Certifique-se de que o terminal esteja visível e que você possa ver o prompt de comando, como:

    ```sh
    usuário@máquina:~/tutorial$
    ```

    :::tip[Atalho de teclado]
    Para alternar a visibilidade do terminal, use <kbd>Ctrl + J</kbd> (macOS: <kbd>Cmd ⌘ + J</kbd>).
    :::

Agora você pode usar o terminal incorporado nesta janela, em vez do Terminal do seu computador, pelo resto deste tutorial.



## Execute o Astro em modo dev

Para visualizar os seus arquivos de projeto _como um website_ enquanto você trabalha, você precisa executar o Astro em modo de desenvolvimento (dev).

### Inicie o servidor dev

1. Execute o comando para iniciar o servidor dev do Astro digitando no terminal do VS Code:

    <PackageManagerTabs>
      <Fragment slot="npm">
      ```shell
      npm run dev
      ```
      </Fragment>
      <Fragment slot="pnpm">
      ```shell
      pnpm run dev
      ```
      </Fragment>
      <Fragment slot="yarn">
      ```shell
      yarn run dev
      ```
      </Fragment>
    </PackageManagerTabs>

    Agora você deve ver a confirmação no terminal de que o Astro está rodando no modo dev. 🚀

## Visualize uma prévia do seu website

Os seus arquivos de projeto contêm todo o código necessário para exibir um website Astro, mas é o navegador que é responsável por exibir o seu código como páginas da web.

1. Clique no link `localhost` na janela do terminal para ver uma prévia ao vivo do seu novo website Astro!

    (O Astro usa `http://localhost:3000` por padrão se a porta 3000 estiver disponível.)

    Aqui está como a prévia do website inicial do projeto Astro ("Empty Project") deve parecer no navegador:

    ![Uma página branca em branco com a palavra Astro no topo.](/tutorial/minimal.png)

:::tip[Usando o servidor dev do Astro]

Enquanto o servidor dev do Astro estiver rodando, você NÃO poderá executar comandos no terminal. Em vez disso, este painel fornecerá feedback enquanto você visualiza o seu site.

Você pode parar o servidor dev a qualquer momento e retornar ao prompt de comando digitando <kbd>Ctrl + C</kbd> no terminal.

Às vezes, o servidor dev para de funcionar sozinho enquanto você está trabalhando. Se a prévia ao vivo parar de funcionar, volte ao terminal e reinicie o servidor dev digitando `npm run dev`.
:::

<Box icon="check-list">
## Lista de verificação

<Checklist>
- [ ] Eu posso criar um novo projeto Astro.
- [ ] Eu posso iniciar o servidor dev do Astro.
</Checklist>
</Box>

### Recursos

- <p>[Iniciando com o Visual Studio Code](https://code.visualstudio.com/docs/introvideos/basics) <Badge>external</Badge> — um tutorial em vídeo para instalar, configurar e trabalhar com o VS Code</p>
